# GitHub Pages hospeda somente o frontend estático.
# A URL publica da API e injetada pelo workflow via variavel do repositorio
# PRESCRIMED_API_URL. O backend deve liberar CORS para o dominio do Pages.
VITE_API_URL=/api
